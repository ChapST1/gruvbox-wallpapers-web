<div class="grid grid-cols-[repeat(auto-fit,minmax(270px,1fr))] gap-3">
    {
        Array(6)
            .fill(null)
            .map((i) => {
                return (
                    <figure class="h-52 w-full animate-pulse rounded-md border border-border-dark flex items-center justify-center" />
                );
            })
    }
</div>

<style scoped>
    div {
        mask-image: linear-gradient(to bottom, #000, transparent);
    }
</style>
